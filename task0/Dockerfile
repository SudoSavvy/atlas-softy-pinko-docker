# Use the latest Ubuntu image
FROM ubuntu:latest

# Update and upgrade the system
RUN apt-get update && apt-get upgrade -y

# Define the command to run when the container starts (using JSON syntax)
CMD ["echo", "Hello, World!"]
